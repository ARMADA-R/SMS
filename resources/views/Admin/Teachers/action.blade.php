<div class="row justify-content-center" style="    flex-wrap: nowrap;">
    <div class="col-lg-12 my-auto">
        <a type="button" class="btn btn-sm btn-info " title="{{trans('app.edit')}}" style="margin: 0px;" id="edit_{{ $id }}" href="{{route('admin.teacher.edit',$id) }}">
            <i class="fas fa-user-edit"></i>
        </a>
    </div>

</div>